// 16진수 구구단

#include <stdio.h>

int main(void)
{
    int a, i;
    scanf("%X", &a);
    for (i = 1; i < 16; i++)  // 16진수로 1씩 증가
        printf("%X*%X=%X\n", a, i, a * i);  // 입력 된 정수 a에 16진수로 1씩 증가하며 곱함
    return 0;
}
