// 44. 정수 3개 입력받아 합과 평균 출력하기
#include <stdio.h>
int main()
{
    int a, b, c;

    scanf("%d %d %d", &a, &b, &c);

    int sum = a + b + c;
    float avg = (float)sum / 3;
    
    printf("%d\n%.1f", sum, avg);
    return 0;
}
