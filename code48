// 48. 두 정수 입력받아 비교하기2
#include <stdio.h>
int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    
    if(a == b)
        printf("True"); // 입력 값 a와 b가 같으면 True 출력
    else
        printf("False"); // 입력 값 a와 b가 같지 않을 경우 False 출력

    return 0;
}
