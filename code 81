// 3 6 9 게임의 왕이 되자!

#include <stdio.h>

int main(void)
{
    int a, i;
    scanf("%d", &a);
    for (i = 1; i <= a; i++) // 입력 된 정수 a만큼 1씩 증가
    {
        if (i % 3 == 0)   // i % 3이 0이 되는 경우
            printf("X ");  // X로 출력
        else
            printf("%d ", i);
    }
    return 0;
}
