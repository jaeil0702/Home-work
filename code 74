// 문자 1개 입력받아 알파벳 출력하기

#include <stdio.h>

int main(void)
{
    int i;
    char a;
    scanf("%c", &a);
    for (i = 'a'; i <= a; i++) // 입력된 문자형 a를 1씩 증가하며 출력
        printf("%c\n", i);
    return 0;
}
