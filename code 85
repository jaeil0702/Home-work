// 여기까지! 이제 그만 ~

#include <stdio.h>

int main(void)
{
    int a, i;
    int sum = 0;
    scanf("%d", &a);
    for (i = 1;; i++) // 1씩 증가
    {
        sum += i;   
        if (sum >= a) // sum값이 a보다 커지거나 같아지면
            break;    // 탈출
    }
    printf("%d", sum);
    return 0;
}
