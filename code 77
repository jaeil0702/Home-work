// 원하는 문자가 입력될 때까지 반복 출력하기

#include <stdio.h>

int main(void)
{
    char a = ' ';   // a를  ' '로 초기화
    while (a != 'q')
    {
        scanf("%c", &a);
        if (a != ' ')       // 공백 제외
            printf("%c\n", a);
    }
    return 0;
}
