// 50. 두 정수 입력받아 비교하기4
#include <stdio.h>
int main()
{
    int a, b;
    scanf("%d %d", &a, &b);

    if(a != b)
        printf("True"); // a와 b가 다른 경우 True를 출력
    else
        printf("False"); // a와 b가 같은 경우 False를 출력
    return 0;
}
