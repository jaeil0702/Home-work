// 정수 1개 입력받아 그대로 출력하기3
#include <stdio.h>

int main()
{
    long long int a; // long long int형은 64bit를 사용하는 정수형
    
    scanf("%lld", &a); // long long int = %lld사용
    printf("%lld", a);
    return 0;
}
