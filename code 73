// 정수 1개 입력받아 카운트다운 출력하기2

#include <stdio.h>

int main(void)
{
    int a;
    scanf("%d", &a);
    a--;
    while (a != -1) // a가 -1이 아닌 경우 while문으로 반복
    {
        printf("%d\n", a); 
        a--; // 입력 된 정수 a를 1씩 감소하며 출력 
    }
    return 0;
}
